<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <!--mysql 连接数据库jar 这里选择自己本地位置-->
    <classPathEntry
            location="D:\m2\repository\mysql\mysql-connector-java\5.1.49\mysql-connector-java-5.1.49.jar"/>


    <context id="MysqlContext" targetRuntime="MyBatis3">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <!-- 指定生成 Mapper 的继承模板 -->
        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <!-- 配置Mapper需要集成的基类，这边是我重新封装的Mapper类，如果不需要则配置默认的都Mapper -->
            <property name="mappers" value="com.example.elmboot.mapper.TkBaseMapper"/>
        </plugin>

        <!-- 支持lombok注释, 去掉set,get方法-->
        <plugin type="com.example.elmboot.config.MyBatisPlugin" >
            <property name="hasLombok" value="true"/>
        </plugin>

        <!-- 自动为entity生成swagger2文档-->
        <plugin type="com.example.elmboot.mybatis.plugins.GeneratorSwagger2Doc">
            <property name="apiModelAnnotationPackage" value="io.swagger.annotations.ApiModel"/>
            <property name="apiModelPropertyAnnotationPackage" value="io.swagger.annotations.ApiModelProperty"/>
        </plugin>

        <commentGenerator>
            <property name="suppressDate" value="true"/>
            <!-- 是否去除自动生成的注释 true：是 ： false:否 -->
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <!-- 数据库连接配置 -->
        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://127.0.0.1:3306/elmboot"
                        userId="root"
                        password="a1b2c3">
            <!--  设置可以获取tables remarks信息 -->
            <property name="useInformationSchema" value="true"/>
            <!-- 设置可以获取remarks信息 -->
            <property name="remarks" value="true"/>
        </jdbcConnection>

        <!-- 生成实体类 -->
        <javaModelGenerator targetPackage="com.example.elmboot.entity" targetProject="src/main/java">
            <property name="trimStrings" value="false"/>
            <property name="enableSubPackages" value="true"/>
        </javaModelGenerator>

        <!-- 生成mapper对应xml -->
        <sqlMapGenerator targetPackage="mapper" targetProject="src/main/resources">
            <property name="enableSubPackages" value="true"/>
        </sqlMapGenerator>

        <!-- 生成mapper文件 -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="com.example.elmboot.mapper" targetProject="src/main/java">
            <property name="enableSubPackages" value="true"/>
        </javaClientGenerator>

        <!-- 指定生成的表 -->
        <!-- 因为TkMybatis已经支持基础操作，因此不再需要对应方法，所以去掉生成 -->
        <table tableName="hospital" enableCountBy="false" enableUpdateBy="false" enableDeleteBy="false"
               enableSelectBy="false" selectByQueryId="false" >
            <generatedKey column="test" sqlStatement="Mysql" identity="true" type="post"/>
        </table>
    </context>
</generatorConfiguration>
